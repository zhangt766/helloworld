N=10
countFound=0
num=30
while countFound<N:
    isPrime=1
    for i in range(2,num-1):
        if (num % i)==0:
            isPrime=0
            break
    if isPrime==1:
        print(num)
        countFound=countFound+1